version: 2
jobs:
  build:
    docker:
     - image: vidbina/ror-react-base:alpha
    working_directory: ~/
    steps:
      - checkout
      - run: mkdir -p /tmp/test-reports
      - run: bundle exec rspec --format progress --format RspecJunitFormatter -o /tmp/test-reports/rspec.xml
      - store_test_results:
          path: /tmp/test-reports
      - 
