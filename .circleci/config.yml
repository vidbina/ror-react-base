jobs:
  build:
    docker:
     - image: 'vidbina/ror-react-base:alpha'
       entrypoint: /bin/bash
       user: root
    working_directory: /src
    steps:
      - run:
          name: Prepare report directory
          command: mkdir -p /tmp/test-reports
      #      - checkout
      - run:
          name: Build test report
          command: bundle exec rspec --format progress --format RspecJunitFormatter -o /tmp/test-reports/rspec.xml
#      - store_test_results:
#        path: /tmp/test-reports
